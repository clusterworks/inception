---

- name: "Proxy - set the Yum proxy"
  lineinfile:
    dest: /etc/yum.conf
    state: present
    regexp: "proxy=.*"
    insertafter: EOF
    line: "proxy={{upstream_proxy}}"
  when: proxy_host != ''

